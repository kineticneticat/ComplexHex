uh so basically, a `controller` decides whether an assembly is valid or not or if an entity is inbounds (shape stuff), and a `complex` does the effects

i.e something when the assembly is created & ended, and something every tick